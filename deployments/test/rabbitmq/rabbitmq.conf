# Note that env variables are also accepted

# Note that guest can only access the server via loopback, so configuring
# a different server may be crucial when switching to the server.

# default_user = guest
# default_password = guest

# default_vhost = /

# Todo: make permissions more restrictive
# default_permissions.configure = .*
# default_permissions.read = .*
# default_permissions.write = .*

listeners.tcp.default = 5672
management.tcp.port = 15672
management.tcp.ip = 0.0.0.0

management.load_definitions = /etc/rabbitmq/definitions.json
